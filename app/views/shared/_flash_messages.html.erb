<% unless flash.empty? %>
    <script>
        <% flash.each do |type, message| %>
          <% type = type.to_s.gsub('danger', 'error').gsub('success', 'info') %>
          toastr["<%= type %>"]("<%= message %>", "<%= type.upcase %>");
        <% end %>
    </script>
<% end %>
