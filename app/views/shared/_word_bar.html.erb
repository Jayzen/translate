<div class='col-md-3'>
    <div class="card">
      <div class="card-header text-secondary text-center">
        单词管理
      </div>
      <div class="card-body d-flex flex-column row-hl">
        <a href="<%= words_lists_path %>">
          <button type="button" class="btn <%= controller_name == 'words' ? 'btn-primary' : 'btn-outline-secondary' %> btn-block mt-2">
            <i class="fa fa-book fa-fw"></i> 单词列表
          </button>
        </a> 
        <a href="<%= tasks_path %>">
          <button type="button" class="btn <%= controller_name == 'tasks' ? 'btn-primary' : 'btn-outline-secondary' %> btn-block mt-2">
            <i class="fa fa-list fa-fw"></i> 单词计划
          </button>
        </a>
        <a href="<%= categories_path %>">
          <button type="button" class="btn <%= controller_name == 'categories' ? 'btn-primary' : 'btn-outline-secondary' %> btn-block mt-2">
            <i class="fa fa-tags fa-fw"></i> 单词分类
          </button>
        </a>
      </div>
    </div>
</div>
